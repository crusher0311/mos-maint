- [Dev Mongo is Prod Mongo](dev-mongo-is-prod.md) — this repl's dev MongoDB == the production cluster; any write/createIndex from dev hits prod live.
- [Long-running jobs need a workflow](long-running-jobs-need-workflow.md) — shell `&`/nohup/setsid jobs are reaped when the bash tool returns; run multi-min backfills via a console workflow (sleep-infinity tail + sentinel), poll getWorkflowStatus.
- [job_index retirement blocked](job-index-retirement-blocked.md) — legacy Mongo job_index still has live readers (job-search fallback, plan-build, parts, dashboard); cannot drop until PG backfill+soak done.
- [Job search PG slowness](job-search-pg-slowness.md) — leading-wildcard ILIKE w/o pg_trgm + created_at-backward-scan explodes on rare/multi/zero-match terms (normalized_service_jobs IS populated; "never wired" comment stale). FIXED: pg_trgm + 3 gin_trgm_ops indexes (25s→sub-sec). No app statement_timeout (Brandon's call).
- [Add-canned-job slowness = post-add reload](canned-job-add-slowness.md) — Tekmetric add feels slow because content-script JOB_CREATED does a 1.5s-delayed full window.location.reload(); the route+upstream POST are sub-second (retry=0). Fix is extension-side (manual publish).
- [Canned jobs empty-cache poisoning](canned-jobs-new-shop-cache.md) — route cached `[]` on any timed-out fetch + hit-check needs length>0 → 0-row row = permanent miss that re-fetches & re-caches empty forever (shops DID have jobs). FIXED: never cache empty + serve stale-ignoring-TTL + 1 retry.
- [DB cutover flips are operator-only](db-cutover-operator-gated.md) — flipping *_PG_CANONICAL, running backfills, soaking, removing fallbacks are prod actions; never do them from an isolated env.
- [Tekmetric drain wedge](tekmetric-drain-wedge.md) — backfills stall when a drain worker hangs on a no-timeout fetch yet keeps refreshing the global lease, blocking the cron fallback.
- [Protractor normalize chunk cost](protractor-normalize-chunk-cost.md) — dense Protractor chunks take 15-20min via serial per-WO normalize (+per-WO single-VIN ACES); the real multi-day-stall cause, NOT a DB hang. Bulk enrichVinsWithAces unused on this path.
- [Background lane starvation](background-lane-starvation.md) — same backfill hangs on the busy WEB process but flies on the idle WORKER; interactive-first + unbounded background wait → silent fleet-wide head-of-line block. Levers: BACKFILL_QUEUE_SHOPS + bounded wait.
- [Backfill worker → Mongo saturation](backfill-worker-mongo-saturation.md) — too-aggressive worker backfill saturates shared Mongo → fleet-wide login/ro-context/cron timeouts (looks like an auth bug). Fast reversible fix: suspend the Render worker (no rebuild), resume gently.
- [normalized_line_items missing index](normalized-line-items-missing-index.md) — collection had only `_id`; per-line-item shadow findOne COLLSCANs 5.2M docs → fleet-wide slowness under backfill. Fix: compound idx {serviceJobId,provenance.sourceIds.idValue}. NORMALIZED_INDEXES map has no lineItems entry.
- [Mongo db-name split](mongo-db-name-split.md) — cron bookkeeping is in db `mos`; app data is in db `mos-maintenance-mvp`; reading cron state via lib/mongo returns empty and fakes a "dead scheduler."
- [Post-deploy cron lock stall](cron-lock-postdeploy-stall.md) — after a web deploy, cron skips "lock held by another instance" for up to ~10 min (killed holder's Mongo lease TTL); expected, self-heals, don't hand-clear.
- [Tekmetric full-page completion](tekmetric-fullpage-completion.md) — `completed` flips only after an optional full-page reindex; that cron head-of-line-blocks on giant shops, starving the rest for days.
- [Full-page cron queue hand-off ordering](fullpage-queue-handoff-ordering.md) — enqueue queue-routed shops in an up-front pass BEFORE deadline-bounded inline work, or giants starve allowlisted/canary shops out of the queue entirely.
- [Operational primitives → PG](operational-primitives-pg.md) — cron lock + Tekmetric rate buckets are transient state; their Mongo→PG cutover is a pure flag flip, NO backfill/soak unlike data stores.
- [Better Stack shared log feed](betterstack-shared-feed.md) — many apps (incl. `heart-helper`) log into one feed; filter to host `mos-maintenance-mvp-main` or you'll see other apps' errors (e.g. RingCentral).
- [Log-sync silent blackout](log-sync-silent-blackout.md) — production_logs feed can freeze while cron shows green; sync jobs must report NEW (not attempted) rows + watch max(dt) freshness, not just run success. NOT a token issue.
- [Backfill completion tracking](backfill-completion-tracking.md) — Tekmetric "done" lives in progress collection's `complete`/`completed` (which disagree), NOT the shops-doc flag; Protractor uses the shops flag. Don't trust one field.
- [Backfill shrink + jobs counter](backfill-ratelimit-shrink.md) — throttled chunks shrink-retry, never FORCE_SKIP (=history gap); classify by 429-backoff accumulator; totalJobsIndexed counts only chunker-committed jobs so bulk-migrated shops falsely show 0.
- [Tekmetric force-skip blast radius](tekmetric-forceskip-blast-radius.md) — WINDOW err (list-page fail): bisect→floor→skip minimal slice; RECORD err (per-RO): advance & record, never skip. Precedence: WINDOW>record, PAGE-CAP-split>record (else unpaged remainder lost).
- [VHI dashboard hang is invisible](vhi-dashboard-hang-observability.md) — dashboard plan page has no upstream timeout/deadline, so true hangs leave NO log; extension has withUpstreamTimeout, dashboard doesn't.
- [Plan-cache degraded entries](plan-cache-degraded-entries.md) — plans built while OEM/DataOne lookup failed must set oemMissing (short TTL + skip-on-read after 30s) or one slow moment poisons the vehicle for 4h; race timers must be cancelled or timeout warns fire on every build.
- [VHI partner response branches](vhi-partner-response-branches.md) — external VHI route returns buckets from 4 branches (cached_plan/stale/analysis_cache/on_demand_build); per-item partner fields must be normalized on ALL of them, not just the in-route formatVhiItem calls.
- [Partner/AppFueled VHI latency](vhi-partner-latency.md) — partner external VHI route has no withUpstreamTimeout (extension does) so it blocks the full rebuild; extension vs partner resolve different mileage (stale vehicles.mileage vs CARFAX est) → 500mi-tolerance cache thrash → repeated 18-26s rebuilds + DataOne timeouts. (vehicles exist but keyed by ObjectId & frozen since one-time import.)
- [Reconcile reopens completed shops](backfill-reconcile-protractor-false-positive.md) — reconcile count must match upstream's date-field semantics per provider AND only re-queue on a real shortfall (directional, ~10% tol, zero-count guard); else completed shops loop forever.
- [WO dual unique keys](normalized-wo-dual-unique.md) — normalized_work_orders has PK id + (shop_id, work_order_number); upsert only guards id, so create paths must resolve by natural key first or 23505 (+ residual concurrent-ingest race).
- [Replit proxy URLs poison package-lock](replit-proxy-urls-in-lockfile.md) — npm installs in Replit write package-firewall.replit.local tarball URLs into the lockfile → Render ENOTFOUND at npm ci; rewrite host to registry.npmjs.org (same integrity); guard now in check-lockfile-sync.
- [Render prod deploy](render-prod-deploy.md) — prod web `mos-tools` + `backfill-drain-worker` AUTO-DEPLOY on push to GitHub main (~7-8min build); `mos-tools-east` is an inactive old web svc. Verify live commit via Render API.
- [VHI/Plan revisit performance](vhi-revisit-performance.md) — a plan-cache HIT still did per-view work; extension needs client SWR cache + no same-RO snap-back, dashboard must store deferredWork & skip live Protractor on hit.
- [Backfill horizon](backfill-horizon.md) — shared env `BACKFILL_HORIZON_YEARS` (PROD=5, default 2); shrink=free instant catch-up lever (biggest); raise=`reopenCompletedShopsForHorizon` guarded by currentChunkEnd>oldestDate.
- [Tekmetric throughput ceilings](tekmetric-throughput-ceilings.md) — page size hard-capped at 100 (probe-confirmed), 10 RPS/key documented; no faster pull w/o shrinking horizon or adding a 2nd API key.
- [Drain worker coverage](drain-worker-coverage.md) — `backfill-drain-worker` drains BOTH protractor+tekmetric (parallel loops); BullMQ v2 worker also has drain-protractor queue; Shop-Ware NOT drained; don't read "drain off" from one lock snapshot.
- [Fleet backfill stall detection](pipeline-stall-detection.md) — pipeline-stall-alerter pages when cron is green but fleet makes zero real progress; signature must exclude lastRunAt, gate on loop liveness, escalate via [OPS-ALERT].
- [Drain watchdog cancels ALL chunks](drain-watchdog-cancels-all.md) — giant-shop post-last-page finalization has no heartbeat, trips the 10-min drain watchdog, which HARD-CANCELs all 4 in-flight chunks (not just the stuck one) → looks like a fleet stall while pages actually flow.
- [Shop-Ware progress collection](shopware-progress-collection.md) — Shop-Ware backfill progress is in collection `ln`, NOT `shopware_backfill_progress` (which chunk-speed-health wrongly reads → its SW branch is dead).
- [Webhook subscription gating](webhook-subscription-gating.md) — auto-subscribe OFF means tekmetric_webhook_subscriptions is empty, so any "missing subscription" check/alert must be gated behind TEKMETRIC_WEBHOOK_AUTO_SUBSCRIBE or it false-positives the whole fleet.
- [direct-db guard gates deploy](direct-db-guard-gates-deploy.md) — `lint:direct-db` runs in Render prebuild; a new file touching Mongo that isn't allowlisted (or in a repo) fails the prod build, not just local lint.
- [/admin vs /platform-admin auth](admin-vs-platform-admin-auth.md) — two separate admin realms with different auth; operator UI placed under /admin silently redirects platform admins to /dashboard. Put it under /platform-admin.
- [AutoFlow DVI write-back path](autoflow-writeback-path.md) — extension writes to AutoFlow via same-origin cookie POST /Admin/dvi_v3/request.php (request_type: update_sheet/save_multi_axle_tire_results/add_rvh); NOT REST/token. DVI source JS is public.
- [AutoFlow DVI extension detection](autoflow-dvi-extension.md) — DVI VIN/mileage live in form-field *values* not innerText; shop id is v3 subdomain vs v4 /shop/<slug>; dual shops resolve as protractor/tekmetric (not autoflow) so anchor on providerHint + match linked provider BY VIN.
- [ACES coverage gaps](aces-coverage-gaps.md) — Protractor history undecoded (fixable), Tek split fixable/VIN-capped, PCDB 0% corpus-wide; SW job_index docs skeletal + provider:"shopware" + source table keyed by tenantId not shopId.
- [DataOne decode is Postgres + backfill poisoning](dataone-decode-is-postgres.md) — job_index ACES decode hits DataOne's *Postgres* (no PG-free path); peak-hours 53300 connection-limit blocks it; a failed batch still stamps acesDecodedAt → poisons resume marker forever. Run off-peak.
- [BullMQ backfill queue](bullmq-backfill-queue.md) — jobId no `:` (use `_`), stable jobId needs removeOnComplete:true, continuation is cron-driven not in-processor, processor must getDb() not null; cron fails OPEN so all break silently.
- [Tekmetric queue lane live but timing out](tekmetric-queue-lane-timeouts.md) — BullMQ+Redis lane IS consuming; "stalled" queued shops are full-page chunks hard-timing-out at 300s on hung/rate-saturated downstream calls (5 retries exhausted), not a dormant queue. Render env-vars API hides env-group REDIS_URL.
- [Protractor list-vs-detail parity](protractor-list-detail-hash-churn.md) — list & detail invoice extraction are content-identical; computeJobHash only flips on line ORDER + float rounding → switching source causes one-time re-index churn. Fix = canonicalize hash.
- [Protractor list-vs-detail line items](protractor-list-richness.md) — `/Invoice/?…` list already carries full lines+deferred (detail N+1 avoidable); `/WorkOrder/?…` list is thin (detail required). Pricing is flat fields, not PriceSummary.
- [Extension button gating](extension-feature-gating.md) — missing AutoFlow buttons usually = feature entitlement off (dvi_prefill/enhance_notes vs oil_sticker) or view-gate (floating hidden on DVI by design), not a bug.
- [Shopmonkey API quirks](shopmonkey-api-quirks.md) — v3 Bearer API-key (no OAuth); locationId often absent & fully optional (key alone authorizes); amounts in CENTS; single-host SPA so extension scrapes company/location from page.
- [Tekmetric ro-context cache slow path](tekmetric-ro-context-cache.md) — keytag/sticker slowness = "incomplete" cache rows (missing vehicleYear/make/model) forcing live API, NOT stale cache; webhooks DO keep it fresh. Enrichment skipped when vin already present. Mongo: shopId is STRING, RO# is workOrderNumber, freshness=updatedAt.
- [Extension dual timeout](extension-dual-timeout.md) — slow extension writes have TWO caps (background `MOS_FETCH_TIMEOUT_MS` + sidepanel `sendMessage` 30s default); widen BOTH, client arg above proxy cap.
- [Extension reliability contract](extension-resilience-contract.md) — features API: transient 503 vs genuine all-off 200 (client keeps last-known-good); captured RO API JSON authoritative over DOM scrape; RO cache keyed shopId:roId; Print tab instant-fill never downgraded; ext 401s (even outside /api/extension/*) must use buildAuthErrorBody or code=none breaks retry classification.
- [Session auth model](session-auth-model.md) — web session = opaque DB token (NO JWT/signing); live store Mongo `sessions` (PG dormant); fast single-IP 401 on dashboard polls = that user's session gone, fix=re-login, not signing/load.
- [Inspect-filter parity](inspect-filter-parity.md) — showInspectItems canonical = preferences.showInspectItems (default true); OE inspect filter lives in dashboard+extension+plan-build and must protect lifetime fluids (diff/trans/etc) on all.
- [Worker power schedule](worker-power-schedule.md) — the 2 background workers auto-suspend weekday-daytime (off 5am–10pm CT Mon–Fri) & run nights+weekends; a paused worker then is EXPECTED, not an outage.
- [Enterprise user-access admin scope](enterprise-user-access-admin-scope.md) — two competing access models unified on shopIds (Model B): UNION read, write full list to ALL of a user's docs; API routes need own authz (page guard ≠ route guard); enterprise lives on SHOP not user; duplicate-doc collapse is deferred/operator-gated.
- [Enterprise user-doc collapse declined](enterprise-user-doc-collapse-declined.md) — don't merge duplicate per-shop users docs: divergent passwords + shopId-only readers (settings/users list) make it regress; app already consistent via union-read.
- [Settings route shop scoping](settings-route-shop-scoping.md) — provider cards must use getSession() (switched shop), not user.shopId (home); also keep smsProvider in sync or the radio shows a stale provider.
- [Data Status "Checking…"](data-status-checking-minmax.md) — count is fast but per-shop date min/max is pathologically slow (filtered-min/max); fix = (shop_id,date) composite indexes on canonical PG (operator-gated).
- [Protractor dates all null](protractor-business-dates-null.md) — Protractor normalized adapter reads top-level date fields but Protractor nests them (Header.*/InvoiceDate) + mapServiceJob never sets completedAt → 0% dates → Data Status panel shows import date. Fix = adapter + fleet re-normalize.
- [Extension shop resolution](extension-shop-resolution.md) — resolve AutoFlow subdomain via shared findShopBySmsId (reads legacy `autoflowDomain` + auto `.autotext.me`); on an explicit-context miss FAIL CLOSED — never fall back to user's primary shop (platform_admin skips access guard → wrong-shop branding).
- [Data Status real dates](data-status-real-dates.md) — panel "oldest" from row created_at = import time, NOT history; work_orders.closed_date carries real history (~99.7%, back to 1990s); vehicles/customers have NO per-record date (mirror RO span for display, but freshness must use their own recency).
- [Connected name & vehicle count](integrations-connected-name-count.md) — Integrations box name (`tekmetric.shopName`) + "imported N vehicles" (`initialSyncVehicles`) are one-time connect-time Mongo caches, not live; wrong name = wrong stored tekmetric.shopId or rebrand (NOT display bug); fixing them adds zero per-render Tekmetric traffic (won't slow VHI).
- [Declined jobs as false anchors](declined-job-anchors.md) — Tekmetric job_index lacked an authorization flag so declined jobs reset interval clocks; per-provider declined signals differ (Tek authorized=false, Protractor isDeferred, Shop-Ware status).
- [Protractor canned-job line pricing](protractor-canned-job-line-pricing.md) — canned/template lines hide price in PriceSummary.SellPrice + labor Rate/Hours, not flat Price; mirror lib/job-index.ts via shared normalizeProtractorPackageLine.
- [Service-key OEM vs history mapping](service-key-oem-vs-history-mapping.md) — "no record" false-miss when OEM toKeyFromName & history toKeyFromFreeText disagree → OEM falls to misc_<id>; fix synonyms in BOTH, keep verb-guarded.
- [Data Status provider detection](data-status-provider-detection.md) — shop provider detectors must check `integrationProvider` FIRST (+ legacy top-level fields) & include it in the Mongo projection; nested `*.configured`/`*.shopId` alone false-reports connected Tekmetric shops as disconnected.
- [Mis-sync purge](shop-mis-sync-purge.md) — fixing a wrong provider connection: disconnect FIRST (stops cron inflow), purge children by parent REF in FK order, split the huge line_items DELETE.
- [VHI entered-odometer anchor](vhi-entered-odometer-anchor.md) — on-screen RO odometer (param `odometer`, content-script `scrapedOdometer`) is TOP of VHI mileage waterfall w/ monotonicity guard; mirror-write to tekmetric_work_orders keeps partner/dashboard consistent. Tekmetric-only.
- [VHI score model](vhi-score-model.md) — score is proportional severity-weighted ratio (soft floor 12, never 0), mirrored in 2 files; pass `upcoming` bucket; "Insufficient History" gray-? is separate via buildApiScore.
- [Drizzle migration path](drizzle-migration-path.md) — `db:generate` is DEAD (journal drift since 0012); ship normalized-PG schema via hand-written `drizzle/00NN.sql` + mirror in `apply-normalized-migration.ts` (`db:migrate:normalized`).
- [Smart backfill timing](smart-backfill-timing.md) — per-shop quiet-window gate from activity logs; `SMART_BACKFILL_TIMING` off/observe/enforce, OFF=no DB read/no log; flipping on is operator-gated like DB cutovers.
- [Extension route response shapes](extension-route-response-shapes.md) — extension API routes don't share one envelope (OCR nests under `result`, vin-decode/plate top-level, deferred-work `{ok,items}`); verify each before wiring sidepanel client. No OCR contract test.
- [Integrator change-propagation lag](integrator-change-propagation.md) — MOS-initiated Protractor writes reach AutoFlow only via Protractor's webhook chain (~2 min) and AppFueled not at all (pull-only); NO outbound MOS->partner fan-out exists.
- [Protractor add-job open-WO](protractor-add-job-open-wo.md) — open Protractor WOs return 0 by OData number + VIN fallback is cache-only & lags after create → add-job 404s; reliable handle = WO GUID from create-work-order (gate by RO# match + UUID-validate).
- [Synonym mining data source](service-key-synonym-mining.md) — interval-import unmatched log is empty (unused feature); mine [PlanBuild] unmatched job names in production_logs; bare one-word synonyms must be exact-equality, mirrored in both matchers.
- [CARFAX match gap logging](carfax-match-logging.md) — unmatched CARFAX descs tallied in-memory per process; fixes need SERVICE_KEYS + toKeyFromName/FreeText kept in sync; operator surfaces under /platform-admin/carfax-match (incl. live manual key overrides).
- [CARFAX inspect-vs-replace anchor](carfax-inspect-vs-replace-anchor.md) — CARFAX desc is "; "-joined multi-phrase w/ verb AFTER noun ("belts checked"); split+verb-guard so an inspection never resets a replace clock (emissions exempt). THREE duplicate anchoring paths (triage, extension overlay, dashboard page) must all guard.
- [Partner absolute URL host resolution](partner-url-host-resolution.md) — every partner-facing absolute URL (reportUrl, serviceIconUrl) must use shared lib/app-host.ts getAppBaseUrl, not NEXT_PUBLIC_BASE_URL, so all URLs share one domain across prod/qa/dev. app-host.ts is pure (client-safe).
- [AutoFlow single-source webhook](autoflow-single-source-webhook.md) — one tokenless `/api/webhooks/autoflow` resolves shop from `shop.domain`→`autoflowDomain`; domain-first + ambiguity-safe (10 near-identical Harrell shops); tokenless = no auth unless AUTOFLOW_SIGNING_SECRET HMAC on.
- [Plan-build VIN-regex COLLSCAN](plan-build-vin-regex-collscan.md) — case-insensitive `$regex` VIN lookups (tekmetric/normalized_work_orders) can't use an index → COLLSCAN → shared-Mongo saturation → ALL sync crons timeout fleet-wide (looks like a broken webhook). Fix = exact `vin.toUpperCase()` match, not regex.
- [Specs/plan cache warming](specs-cache-warming.md) — warm dataone_cache (7d TTL, squish-keyed, free) not cached_plans (4h TTL=moot nightly); never warm CARFAX (costs $); getMaintenanceScheduleCached takes FULL vin.
- [New-shop backfill priority already exists](newshop-backfill-priority.md) — Tekmetric already bumps new shops (newShops fastpath <14d + never-started bucket); "empty/thin" new shops are usually genuinely empty at SOURCE — verify getRepairOrders totalElements before bumping (job_index count > source RO count = caught up).
- [Ambiguous-VIN specs](ambiguous-vin-specs.md) — squish-ambiguous VINs must serve candidate-intersected specs, not refuse; plus client !data-gated fetch effects loop forever on ok:false.
- [SW integrator_tags unavailable](sw-integrator-tags-unavailable.md) — SW line-level PCDB never populates: no connected shop runs a PartsTech catalog (shop tags []); cache-zero is real but backfill-base-assoc; dev creds sandbox-only so no live prod verify.
- [Enterprise job-search candidate fairness](job-search-enterprise-fairness.md) — BOTH arms must pick candidates PER-SHOP or the busiest/newest-ingested shop starves other locations; crucially the MONGO arm serves enterprise (PG exceeds the 1.2s grace @ ~16s) so fixing only PG does nothing; PG=one window query (pool max:2), Mongo=per-shop parallel fan-out + round-robin (no DB $sort).
- [Job-search ACES tiers & ranking](job-scoring-aces-tiers.md) — ACES ids are pass/fail short-circuit tiers (not graded %) AND re-decoded LIVE from VIN at query time, NOT read from the backfilled columns; heuristic can hit 100 for a diff-model donor → capped + sort tie-broken so Exact Fit beats Great Match.
- [Webhook received but jobs never indexed](webhook-indexed-jobs-gap.md) — RO can arrive fully via webhook (cache row has vin+odometer+data.jobs) yet job_index stays empty (jobsIndexed unset) → plan/last-performed falls back to CARFAX; terminal-index gate on cached.vin races enrichment for new customers; display RO# ≠ internal id.
- [server-only blocks tsx tests](server-only-untestable-under-tsx.md) — a module importing "server-only" throws under tsx; extract pure logic into a no-server-only sibling to unit-test it.
- [Dashboard/KB search index-friendly](dashboard-search-index-friendly.md) — dashboard Mongo search anchored to prefix (VIN upper/case-sensitive) via lib/dashboard-search.ts; KB ILIKE relies on pg_trgm; indexes operator-gated (ensure-indexes.ts + drizzle/0021).
- [Recovery vs stale plan cache](recovery-vs-plan-cache-staleness.md) — "still shows CARFAX" after a history re-file = stale 4h cached_plans (deploy does NOT clear it); recovered job_index rows use performedAt only, but readers DO include it so shop IS credited on rebuild.
- [Interval-import PDF paths](interval-import-pdf-paths.md) — gpt-4o PDF file-part OCR is lossier than a photo; digital PDFs must go through the unpdf text-layer path; dev `DEV_AUTO_LOGIN=true` lets unauthenticated curls hit protected routes.
- [Declined work in VHI](declined-work-in-vhi.md) — Tekmetric declined jobs merge into plan-build (forced overdue, `declined` provenance); add-all WRITES happen extension-side via page session, server only lists/resolves/dedups.
- [Service-key synonym false positives](service-key-synonym-false-positives.md) — substring synonyms cross-match ("coil"→oil, cabin→engine_air, fob battery); corpus-check new synonyms via scripts/probe-carfax-phrase-corpus.ts.
